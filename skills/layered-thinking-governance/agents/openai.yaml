interface:
  display_name: "Layered Thinking Governance"
  short_description: "按物事器术法分层，保持权限边界并要求所有窗口定期向上复核目标"
  default_prompt: "Use $layered-thinking-governance to classify the current problem by layer, perform an upward review of one or two levels without expanding decision authority, detect goal drift, and explain any escalation in plain Chinese."

policy:
  allow_implicit_invocation: true
